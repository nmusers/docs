baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'NONMEM documentations'
theme = 'hugo-book'

[markup.goldmark.renderer]
  unsafe = true

[caches]
  [caches.images]
    dir = ':cacheDir/images'

# (Optional) Set this to true to enable 'Last Modified by' date and git author
#  information on 'doc' type pages.
enableGitInfo = true
